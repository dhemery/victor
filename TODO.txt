Frank API:
   - Implement app_exec
   - Implement accessibility_check
   - Implement dump

Environment checks, with helpful error messages:
   - Assistive access enabled in the Universal Access system preferences.
   - Accessibility enabled in the simulator.

Polling
   - Phone conditions

General API features:
   - thing.when(Condition) => ThingCommands
   - thing.waitUntil(Condition)
   - Application API (e.g. to get elements)?

Phone:
   - API for phone-related simulator menus 

Simulator:
   - API for simulation-related menus 

Research:
   - Queries that Frank can execute
   - Methods that Frank can invoke on query results (targets)
